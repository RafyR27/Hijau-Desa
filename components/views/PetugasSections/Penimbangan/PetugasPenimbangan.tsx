import { SessionUser } from "@/types/user";

const PetugasPenimbangan = ({ user }: { user?: SessionUser }) => {
  return (
    <div>
      {/* View Penimbangan Petugas */}
    </div>
  );
};

export default PetugasPenimbangan;
